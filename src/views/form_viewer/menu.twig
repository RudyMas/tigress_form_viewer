{% extends 'base.twig' %}
{% block css %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ BASE_URL }}/vendor/tigress/menu/public/css/tiles.css">
{% endblock %}
{% block title %}{{ __('Forms') }}{% endblock %}
{% block breadcrumb %}
    <div class="row mb-2">
        <div class="col-md-12">
            <nav aria-label="breadcrumb" data-bs-theme="{{ WEBSITE.mode }}">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a href="{{ BASE_URL }}/"><i class="fa fa-home"></i></a></li>
                    <li class="breadcrumb-item active">{{ __('Forms') }}</li>
                </ol>
            </nav>
        </div>
    </div>
{% endblock %}
{% block content %}
    <div class="container">
        <div class="row mb-2">
            <div class="col-md-12">
                <h3>{{ __('Forms') }}
                    {% if (adminAccess) %}
                        <a class="btn btn-sm btn-outline-light" href="{{ BASE_URL }}/form-viewer/settings">
                            <i class="fa-solid fa-sliders"></i>
                        </a>
                    {% endif %}
                </h3>
            </div>
        </div>
        <div class="row">
            <div class="col-md-12">
                <p>{{ content|raw }}</p>
            </div>
        </div>
    </div>
{% endblock %}